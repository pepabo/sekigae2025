# sekigae2025 amber-lang ver

Amber https://amber-lang.com/ is a programming language compiled into Bash.

You can compile and run it as follows:

```bash
amber build main.ab
bash main.sh
```

Or you can run it directly as follows:

```bash
amber run main.rb
```
